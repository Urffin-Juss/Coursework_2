# Проект: Работа с вакансиями hh.ru

## 📌 Описание проекта

Программа получает информацию о вакансиях с платформы [hh.ru](https://hh.ru) с помощью публичного API, сохраняет их в JSON-файл и позволяет пользователю работать с ними через консольный интерфейс:

- искать вакансии по запросу;
- получать **топ N** вакансий по зарплате;
- фильтровать вакансии по ключевому слову в названии или описании;
- сохранять и удалять вакансии из файла.

Проект разработан в парадигме **ООП** с соблюдением принципов **наследования**, **инкапсуляции** и **SOLID**.

---


---

## 🧩 Основные классы и модули

### `api_request.py`
- `BaseAPI` — абстрактный класс для работы с API (использует `@abstractmethod`).
- `HHAPI` — реализация для hh.ru. Получает вакансии с помощью GET-запросов, обрабатывает ответы, проверяет статус и возвращает список вакансий.

### `vacancy.py`
- `Vacancy` — класс, описывающий вакансию.
- Поля: `title`, `url`, `salary`, `description`, `employer`.
- Реализована валидация зарплаты (если не указана или отрицательная — подставляется 0).
- Методы сравнения (`__lt__`, `__gt__`) для сортировки по зарплате.
- Классметод `from_hh()` создаёт экземпляр на основе данных от API.

### `storage.py`
- `BaseStorage` — абстрактное хранилище с методами `save`, `load`, `add`, `delete`.
- `JSONStorage` — реализация, сохраняющая вакансии в JSON-файл в папке `data/`.

### `cli.py`
- Функции для взаимодействия с пользователем через консоль:
  - поиск вакансий по запросу;
  - отображение топ N по зарплате;
  - фильтрация по ключевому слову;
  - сохранение и удаление вакансий;
- Использует классы `HHAPI`, `Vacancy`, `JSONStorage`.

---

## 🧠 Принципы ООП и SOLID

- **Наследование:** `HHAPI` ← `BaseAPI`, `JSONStorage` ← `BaseStorage`.
- **Инкапсуляция:** приватные атрибуты (`_filepath`, `_base_url`).
- **Полиморфизм:** одинаковые методы `save_vacancies`, `load_vacancies` для разных реализаций.
- **Абстракция:** интерфейсы через абстрактные классы (`ABC`, `abstractmethod`).
- **Single Responsibility Principle:** каждый класс отвечает только за свою задачу.

---

## 💻 Пример работы программы

```text
=== Поиск вакансий с hh.ru ===
Введите поисковый запрос (например, 'python разработчик'): python
Получаю вакансии с hh.ru ...
Найдено вакансий: 50
Вакансии сохранены в data/vacancies.json

Выберите действие:
1 — показать топ N вакансий по зарплате
2 — отфильтровать вакансии по ключевому слову
3 — показать все вакансии
0 — выход

Ваш выбор: 1
Сколько вакансий показать? 5
1. Python Developer — 300000 руб. — https://hh.ru/vacancy/12345
2. Backend Engineer — 250000 руб. — https://hh.ru/vacancy/67890
...


